---
description: Reception project context for impeccable design sessions — read before any /impeccable command
alwaysApply: true
---

# Reception × Impeccable

This project uses the **impeccable** skill for landing page design work on `index.html`.

## Session boot (required)

When the user invokes `/impeccable` or any impeccable sub-command:

1. Run `node .cursor/skills/impeccable/scripts/context.mjs` once per session
2. Read `.impeccable/PROGRESS.md` — session handoff with completed passes and pending next steps
3. Read the latest critique in `.impeccable/critique/` if doing evaluate/refine work
4. Read `reference/brand.md` (register is `brand` per `PRODUCT.md`)

## Project snapshot

- **Reception** — missed-call WhatsApp automation LP for solo professionals
- **North star:** "The Clear Signal" — trusted local service, not generic SaaS
- **Canonical tokens:** `index.html` `:root` (spruce green, Zilla Slab + Libre Franklin)
- **Next command:** `/impeccable layout index.html` (header + sticky nav)

Do not restart from scratch. Continue the pending queue in `.impeccable/PROGRESS.md` unless the user changes direction.
